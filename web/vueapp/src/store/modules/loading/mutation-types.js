/* ============
 * Mutation types for the loading module
 * ============
 *
 * The mutation types that are available
 * on the loading module.
 */

export const SET = 'SET';
export const UNSET = 'UNSET';

export default {
  SET,
  UNSET,
};
